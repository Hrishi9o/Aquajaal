{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "installCommand": "if cd flutter; then git pull && cd .. ; else git clone https://github.com/flutter/flutter.git --depth 1 -b stable flutter; fi && flutter/bin/flutter config --enable-web",
  "buildCommand": "flutter/bin/flutter build web --release --no-tree-shake-icons",
  "outputDirectory": "build/web",
  "cleanUrls": true,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
